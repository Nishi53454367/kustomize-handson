apiVersion: kustomize.config.k8s.io/v1beta1
kind: Deployment
metadata:
  name: sample
spec:
  template:
    metadata:
      labels:
        app: sample
    spec:
      containers:
      - name: nginx
        image: nginx:alpine